/**
 * Zeigt eine Meldung, wenn ein Button/Link noch keine Funktion hat.
 * Projektweit für alle nicht implementierten Aktionen verwenden.
 */
export function nochNichtImplementiert(): void {
  alert("Diese Funktion ist noch nicht implementiert.");
}
